{% extends 'base.html' %}

{% block title %}删除确认 - {{ transfer.transfer_number }} - BetterLaser ERP{% endblock %}

{% block breadcrumb %}
<span class="mx-2 text-gray-400">/</span>
<span class="text-gray-500">库存管理</span>
<span class="mx-2 text-gray-400">/</span>
<a href="{% url 'inventory:transfer_list' %}" class="text-gray-500 hover:text-gray-700">库存调拨单</a>
<span class="mx-2 text-gray-400">/</span>
<a href="{% url 'inventory:transfer_detail' transfer.pk %}" class="text-gray-500 hover:text-gray-700">{{ transfer.transfer_number }}</a>
<span class="mx-2 text-gray-400">/</span>
<span class="text-gray-900 font-semibold">删除确认</span>
{% endblock %}

{% block content %}
<div class="max-w-3xl mx-auto space-y-6">
    <div class="bg-white rounded-lg shadow overflow-hidden">
        <div class="px-6 py-4 bg-red-50 border-b border-red-200">
            <div class="flex items-center">
                <i class="fas fa-exclamation-triangle text-red-600 text-2xl mr-3"></i>
                <div>
                    <h3 class="text-lg font-semibold text-red-900">确认删除</h3>
                    <p class="text-sm text-red-700 mt-1">此操作将删除以下库存调拨单及其所有明细</p>
                </div>
            </div>
        </div> <div class="p-6">
            <div class="space-y-4">
                <div class="flex justify-between py-2 border-b border-gray-200">
                    <span class="text-sm font-medium text-gray-500">调拨单号</span>
                    <span class="text-sm font-semibold text-gray-900">{{ transfer.transfer_number }}</span>
                </div>
                <div class="flex justify-between py-2 border-b border-gray-200">
                    <span class="text-sm font-medium text-gray-500">源仓库</span>
                    <span class="text-sm text-gray-900">{{ transfer.from_warehouse.name }}</span>
                </div>
                <div class="flex justify-between py-2 border-b border-gray-200">
                    <span class="text-sm font-medium text-gray-500">目标仓库</span>
                    <span class="text-sm text-gray-900">{{ transfer.to_warehouse.name }}</span>
                </div>
                <div class="flex justify-between py-2 border-b border-gray-200">
                    <span class="text-sm font-medium text-gray-500">调拨日期</span>
                    <span class="text-sm text-gray-900">{{ transfer.transfer_date }}</span>
                </div>
                <div class="flex justify-between py-2 border-b border-gray-200">
                    <span class="text-sm font-medium text-gray-500">明细数量</span>
                    <span class="text-sm text-gray-900">{{ items|length }} 项</span>
                </div>
            </div> <div class="mt-6 p-4 bg-yellow-50 border-l-4 border-yellow-400 rounded">
                <div class="flex">
                    <i class="fas fa-exclamation-circle text-yellow-600 mt-0.5 mr-3"></i>
                    <div class="text-sm text-yellow-800">
                        <strong>注意：</strong>删除后此调拨单将无法恢复。
                    </div>
                </div>
            </div> <form method="post" class="mt-6">
                {% csrf_token %}
                <div class="flex justify-end space-x-3">
                    <a href="{% url 'inventory:transfer_detail' transfer.pk %}"
                       class="px-6 py-2 border border-gray-300 rounded-lg text-gray-700 hover:bg-gray-50">
                        <i class="fas fa-times mr-2"></i>取消
                    </a>
                    <button class="btn btn-primary" type="submit">
                        <i class="fas fa-trash mr-2"></i>确认删除
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>
{% endblock %}
