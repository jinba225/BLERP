{% extends 'base.html' %}

{% block title %}删除确认 - {{ outbound.order_number }} - BetterLaser ERP{% endblock %}

{% block breadcrumb %}
<span class="mx-2 text-gray-400">/</span>
<span class="text-gray-500">库存管理</span>
<span class="mx-2 text-gray-400">/</span>
<a href="{% url 'inventory:outbound_list' %}" class="text-gray-500 hover:text-gray-700">出库单</a>
<span class="mx-2 text-gray-400">/</span>
<a href="{% url 'inventory:outbound_detail' outbound.pk %}" class="text-gray-500 hover:text-gray-700">{{ outbound.order_number }}</a>
<span class="mx-2 text-gray-400">/</span>
<span class="text-gray-900 font-semibold">删除确认</span>
{% endblock %}

{% block content %}
<div class="max-w-3xl mx-auto space-y-6">
    <div class="bg-white rounded-lg shadow overflow-hidden">
        <div class="px-6 py-4 bg-red-50 border-b border-red-200">
            <div class="flex items-center">
                <i class="fas fa-exclamation-triangle text-red-600 text-2xl mr-3"></i>
                <div>
                    <h3 class="text-lg font-semibold text-red-900">删除出库单</h3>
                    <p class="text-sm text-red-700 mt-1">确认要删除此出库单吗？</p>
                </div>
            </div>
        </div> <div class="p-6">
            <div class="space-y-4">
                <div class="flex justify-between py-2 border-b border-gray-200">
                    <span class="text-sm font-medium text-gray-500">出库单号</span>
                    <span class="text-sm font-semibold text-gray-900">{{ outbound.order_number }}</span>
                </div>
                <div class="flex justify-between py-2 border-b border-gray-200">
                    <span class="text-sm font-medium text-gray-500">出库类型</span>
                    <span class="text-sm text-gray-900">{{ outbound.get_order_type_display }}</span>
                </div>
                <div class="flex justify-between py-2 border-b border-gray-200">
                    <span class="text-sm font-medium text-gray-500">仓库</span>
                    <span class="text-sm text-gray-900">{{ outbound.warehouse.name }}</span>
                </div>
                <div class="flex justify-between py-2 border-b border-gray-200">
                    <span class="text-sm font-medium text-gray-500">出库日期</span>
                    <span class="text-sm text-gray-900">{{ outbound.order_date }}</span>
                </div>
                <div class="flex justify-between py-2 border-b border-gray-200">
                    <span class="text-sm font-medium text-gray-500">明细数量</span>
                    <span class="text-sm text-gray-900">{{ items|length }} 项</span>
                </div>
            </div> <div class="mt-6 p-4 bg-red-50 border-l-4 border-red-400 rounded">
                <div class="flex">
                    <i class="fas fa-exclamation-circle text-red-600 mt-0.5 mr-3"></i>
                    <div class="text-sm text-red-800">
                        <strong>删除后将执行：</strong>
                        <ul class="mt-2 list-disc list-inside space-y-1">
                            <li>出库单将被标记为已删除</li>
                            <li>无法恢复此记录</li>
                            <li>不会影响已有的库存数据</li>
                        </ul>
                    </div>
                </div>
            </div> <form method="post" class="mt-6">
                {% csrf_token %}
                <div class="flex justify-end space-x-3">
                    <a href="{% url 'inventory:outbound_detail' outbound.pk %}"
                       class="px-6 py-2 border border-gray-300 rounded-lg text-gray-700 hover:bg-gray-50">
                        <i class="fas fa-arrow-left mr-2"></i>返回
                    </a>
                    <button class="btn btn-primary" type="submit">
                        <i class="fas fa-trash mr-2"></i>确认删除
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>
{% endblock %}
