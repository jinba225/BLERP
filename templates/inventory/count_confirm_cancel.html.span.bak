{% extends 'base.html' %}

{% block title %}取消确认 - {{ count.count_number }} - BetterLaser ERP{% endblock %}

{% block breadcrumb %}
<span class="mx-2 text-gray-400">/</span>
<span class="text-gray-500">库存管理</span>
<span class="mx-2 text-gray-400">/</span>
<a href="{% url 'inventory:count_list' %}" class="text-gray-500 hover:text-gray-700">库存盘点</a>
<span class="mx-2 text-gray-400">/</span>
<a href="{% url 'inventory:count_detail' count.pk %}" class="text-gray-500 hover:text-gray-700">{{ count.count_number }}</a>
<span class="mx-2 text-gray-400">/</span>
<span class="text-gray-900 font-semibold">取消确认</span>
{% endblock %}

{% block content %}
<div class="max-w-3xl mx-auto space-y-6">
    <div class="bg-white rounded-lg shadow overflow-hidden">
        <div class="px-6 py-4 bg-orange-50 border-b border-orange-200">
            <div class="flex items-center">
                <svg x-data="heroicon('x-circle')" class="text-orange-600 text-2xl mr-3" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" x-cloak><span x-html="svg"></span></svg>
                <div>
                    <h3 class="text-lg font-semibold text-orange-900">取消盘点单</h3>
                    <p class="text-sm text-orange-700 mt-1">确认要取消此盘点单吗？</p>
                </div>
            </div>
        </div> <div class="p-6">
            <div class="space-y-4">
                <div class="flex justify-between py-2 border-b border-gray-200">
                    <span class="text-sm font-medium text-gray-500">盘点单号</span>
                    <span class="text-sm font-semibold text-gray-900">{{ count.count_number }}</span>
                </div>
                <div class="flex justify-between py-2 border-b border-gray-200">
                    <span class="text-sm font-medium text-gray-500">盘点类型</span>
                    <span class="text-sm text-gray-900">{{ count.get_count_type_display }}</span>
                </div>
                <div class="flex justify-between py-2 border-b border-gray-200">
                    <span class="text-sm font-medium text-gray-500">仓库</span>
                    <span class="text-sm text-gray-900">{{ count.warehouse.name }}</span>
                </div>
                <div class="flex justify-between py-2 border-b border-gray-200">
                    <span class="text-sm font-medium text-gray-500">当前状态</span>
                    <span class="px-2 py-1 text-xs font-semibold rounded
                        {% if count.status == 'in_progress' %}bg-theme-100 text-theme-800
                        {% elif count.status == 'planned' %}bg-yellow-100 text-yellow-800
                        {% else %}bg-gray-100 text-gray-800{% endif %}">
                        {{ count.get_status_display }}
                    </span>
                </div>
                <div class="flex justify-between py-2 border-b border-gray-200">
                    <span class="text-sm font-medium text-gray-500">明细数量</span>
                    <span class="text-sm text-gray-900">{{ items|length }} 项</span>
                </div>
            </div> <div class="mt-6 p-4 bg-orange-50 border-l-4 border-orange-400 rounded">
                <div class="flex">
                    <svg x-data="heroicon('exclamation-circle')" class="text-orange-600 mt-0.5 mr-3" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" x-cloak><span x-html="svg"></span></svg>
                    <div class="text-sm text-orange-800">
                        <strong>取消后将执行：</strong>
                        <ul class="mt-2 list-disc list-inside space-y-1">
                            <li>盘点单状态变更为"已取消"</li>
                            <li>无法再进行任何操作</li>
                            <li>不会影响已有的库存数据</li>
                            <li>已填写的盘点数据将被保留</li>
                        </ul>
                    </div>
                </div>
            </div> <form method="post" class="mt-6">
                {% csrf_token %}
                <div class="flex justify-end space-x-3">
                    <a href="{% url 'inventory:count_detail' count.pk %}"
                       class="px-6 py-2 border border-gray-300 rounded-lg text-gray-700 hover:bg-gray-50">
                        <svg x-data="heroicon('arrow-left')" class="mr-2" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" x-cloak><span x-html="svg"></span></svg>返回
                    </a>
                    <button class="btn btn-primary" type="submit">
                        <svg x-data="heroicon('x-mark')" class="mr-2" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" x-cloak><span x-html="svg"></span></svg>确认取消
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>
{% endblock %}
