{% extends 'base.html' %}
{% block title %}导入打印模板 - BetterLaser ERP{% endblock %}

{% block breadcrumb %}
<span class="mx-2 text-gray-400">/</span>
<span class="text-gray-500">基础设置</span>
<span class="mx-2 text-gray-400">/</span>
<a href="{% url 'core:print_template_list' %}" class="text-gray-500 hover:text-gray-700">打印模板</a>
<span class="mx-2 text-gray-400">/</span>
<span class="text-gray-900 font-semibold">导入模板</span>
{% endblock %}

{% block content %}
<div class="max-w-2xl mx-auto">
    <form method="post" enctype="multipart/form-data">
        {% csrf_token %} <div class="flex justify-between items-center mb-6">
            <h3 class="text-lg font-semibold text-gray-900">
                <i class="fas fa-file-import mr-2"></i>导入打印模板
            </h3>
            <div class="flex space-x-3">
                <a href="{% url 'core:print_template_list' %}" class="px-4 py-2 border border-gray-300 text-gray-700 rounded-lg hover:bg-gray-50">
                    <svg x-data="heroicon('x-mark')" class="mr-2" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" x-cloak><span x-html="svg"></span></svg>取消
                </a>
                <button class="btn btn-primary" type="submit">
                    <i class="fas fa-file-import mr-2"></i>导入模板
                </button>
            </div>
        </div> <div class="bg-white rounded-lg shadow p-6 space-y-6">
            <div>
                <label class="block text-sm font-medium text-gray-700 mb-2">选择模板文件 *</label>
                <input
                    type="file"
                    name="import_file"
                    accept=".json,application/json"
                    required
                    class="w-full px-3 py-2 border border-gray-300 rounded-lg"
                >
                <p class="mt-1 text-xs text-gray-500">支持 JSON 格式的模板文件</p>
            </div> <div class="bg-theme-50 border border-theme-200 rounded-lg p-4">
                <div class="flex">
                    <svg x-data="heroicon('information-circle')" class="text-theme-600 mt-0.5 mr-3" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" x-cloak><span x-html="svg"></span></svg>
                    <div class="text-sm text-theme-800">
                        <p class="font-medium mb-2">导入说明：</p>
                        <ul class="list-disc list-inside space-y-1 text-xs">
                            <li>仅支持从本系统导出的 JSON 格式模板文件</li>
                            <li>导入的模板名称会自动添加"(导入)"后缀</li>
                            <li>导入的模板不会自动设为默认，需要手动设置</li>
                            <li>导入后可以编辑模板进行个性化调整</li>
                        </ul>
                    </div>
                </div>
            </div> <div class="bg-yellow-50 border border-yellow-200 rounded-lg p-4">
                <div class="flex">
                    <svg x-data="heroicon('exclamation-triangle')" class="text-yellow-600 mt-0.5 mr-3" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" x-cloak><span x-html="svg"></span></svg>
                    <div class="text-sm text-yellow-800">
                        <p class="font-medium mb-2">注意事项：</p>
                        <ul class="list-disc list-inside space-y-1 text-xs">
                            <li>确保文件来源可信，避免导入来路不明的模板文件</li>
                            <li>导入前建议先导出现有模板作为备份</li>
                            <li>如果导入失败，请检查文件格式是否正确</li>
                        </ul>
                    </div>
                </div>
            </div>
        </div>
    </form> <!-- 导入步骤说明 -->
    <div class="mt-6 bg-white rounded-lg shadow p-6">
        <h4 class="text-md font-semibold text-gray-900 mb-4">
            <i class="fas fa-list-ol mr-2"></i>如何导入模板
        </h4>
        <div class="space-y-4">
            <div class="flex">
                <div class="flex-shrink-0 w-8 h-8 bg-theme-100 text-theme-600 rounded-full flex items-center justify-center font-semibold text-sm mr-3">
                    1
                </div>
                <div>
                    <p class="text-sm font-medium text-gray-900">准备模板文件</p>
                    <p class="text-xs text-gray-500 mt-1">从其他系统导出的 JSON 格式模板文件，或从本系统模板管理页面导出的文件</p>
                </div>
            </div>
            <div class="flex">
                <div class="flex-shrink-0 w-8 h-8 bg-theme-100 text-theme-600 rounded-full flex items-center justify-center font-semibold text-sm mr-3">
                    2
                </div>
                <div>
                    <p class="text-sm font-medium text-gray-900">选择文件</p>
                    <p class="text-xs text-gray-500 mt-1">点击"选择文件"按钮，选择要导入的 JSON 文件</p>
                </div>
            </div>
            <div class="flex">
                <div class="flex-shrink-0 w-8 h-8 bg-theme-100 text-theme-600 rounded-full flex items-center justify-center font-semibold text-sm mr-3">
                    3
                </div>
                <div>
                    <p class="text-sm font-medium text-gray-900">导入并编辑</p>
                    <p class="text-xs text-gray-500 mt-1">点击"导入模板"按钮，系统会自动跳转到模板编辑页面，可以进行进一步调整</p>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
