{% extends 'base.html' %}

{% block title %}AI模型配置 - BetterLaser ERP{% endblock %}

{% block breadcrumb %}
<span class="mx-2 text-gray-400">/</span>
<span class="text-gray-500">系统设置</span>
<span class="mx-2 text-gray-400">/</span>
<span class="text-gray-900 font-semibold">AI模型配置</span>
{% endblock %}

{% block content %}
<div class="space-y-6">
    <!-- Header -->
    <div class="flex justify-between items-center">
        <div>
            <h3 class="text-lg font-semibold text-gray-900">🤖 AI模型配置管理</h3>
            <p class="text-sm text-gray-600">共 {{ total_count }} 个配置，{{ active_count }} 个启用中</p>
        </div>
        <a class="btn btn-primary" href="{% url 'ai_assistant:model_config_create' %}">
            <svg x-data="heroicon('plus')" class="mr-2" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" x-cloak><span x-html="svg"></span></svg>新建配置
        </a>
    </div> <!-- Stats Cards -->
    <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
        <div class="bg-white rounded-lg shadow p-6">
            <div class="flex items-center">
                <div class="flex-shrink-0">
                    <div class="w-12 h-12 bg-theme-100 rounded-lg flex items-center justify-center">
                        <i class="fas fa-robot text-theme-600 text-xl"></i>
                    </div>
                </div>
                <div class="ml-4">
                    <p class="text-sm font-medium text-gray-600">总配置数</p>
                    <p class="text-2xl font-semibold text-gray-900">{{ total_count }}</p>
                </div>
            </div>
        </div>
        <div class="bg-white rounded-lg shadow p-6">
            <div class="flex items-center">
                <div class="flex-shrink-0">
                    <div class="w-12 h-12 bg-green-100 rounded-lg flex items-center justify-center">
                        <svg x-data="heroicon('check-circle')" class="text-green-600 text-xl" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" x-cloak><span x-html="svg"></span></svg>
                    </div>
                </div>
                <div class="ml-4">
                    <p class="text-sm font-medium text-gray-600">启用中</p>
                    <p class="text-2xl font-semibold text-gray-900">{{ active_count }}</p>
                </div>
            </div>
        </div>
        <div class="bg-white rounded-lg shadow p-6">
            <div class="flex items-center">
                <div class="flex-shrink-0">
                    <div class="w-12 h-12 bg-yellow-100 rounded-lg flex items-center justify-center">
                        <svg x-data="heroicon('star')" class="text-yellow-600 text-xl" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" x-cloak><span x-html="svg"></span></svg>
                    </div>
                </div>
                <div class="ml-4">
                    <p class="text-sm font-medium text-gray-600">默认配置</p>
                    <p class="text-lg font-semibold text-gray-900">
                        {% if default_config %}
                            {{ default_config.name }}
                        {% else %}
                            <span class="text-gray-400">未设置</span>
                        {% endif %}
                    </p>
                </div>
            </div>
        </div>
    </div> <!-- Search and Filter -->
    <div class="bg-white rounded-lg shadow p-6">
        <form method="get" class="grid grid-cols-1 md:grid-cols-4 gap-4">
            <div>
                <label class="block text-sm font-medium text-gray-700 mb-2">搜索</label>
                <input type="text" name="search" value="{{ search }}"
                       class="w-full px-3 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-theme-500 focus:border-theme-500 transition-colors"
                       placeholder="配置名称...">
            </div>
            <div>
                <label class="block text-sm font-medium text-gray-700 mb-2">提供商</label>
                <select name="provider"
                        class="w-full px-3 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-theme-500 focus:border-theme-500 transition-colors">
                    <option value="">全部</option>
                    {% for value, label in provider_choices %}
                    <option value="{{ value }}" {% if current_provider == value %}selected{% endif %}>
                        {{ label }}
                    </option>
                    {% endfor %}
                </select>
            </div>
            <div>
                <label class="block text-sm font-medium text-gray-700 mb-2">状态</label>
                <select name="is_active"
                        class="w-full px-3 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-theme-500 focus:border-theme-500 transition-colors">
                    <option value="">全部</option>
                    <option value="true" {% if current_is_active == 'true' %}selected{% endif %}>启用</option>
                    <option value="false" {% if current_is_active == 'false' %}selected{% endif %}>禁用</option>
                </select>
            </div>
            <div class="flex items-end">
                <button class="btn btn-primary" type="submit">
                    <svg x-data="heroicon('magnifying-glass')" class="mr-2" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" x-cloak><span x-html="svg"></span></svg>搜索
                </button>
            </div>
        </form>
    </div> <!-- Configs Table -->
    <div class="bg-white rounded-lg shadow overflow-x-auto">
        <table class="min-w-full divide-y divide-gray-200">
            <thead class="bg-gray-50">
                <tr>
                    <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">配置名称</th>
                    <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">提供商</th>
                    <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">模型名称</th>
                    <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">参数</th>
                    <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">使用统计</th>
                    <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">状态</th>
                    <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">操作</th>
                </tr>
            </thead>
            <tbody class="bg-white divide-y divide-gray-200">
                {% for config in page_obj %}
                <tr class="hover:bg-gray-50 transition-colors">
                    <td class="px-6 py-4 whitespace-nowrap">
                        <div class="flex items-center">
                            {% if config.is_default %}
                            <svg x-data="heroicon('star')" class="text-yellow-500 mr-2" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" x-cloak><span x-html="svg"></span></svg>
                            {% endif %}
                            <div>
                                <div class="text-sm font-medium text-gray-900">{{ config.name }}</div>
                                {% if config.description %}
                                <div class="text-xs text-gray-500">{{ config.description|truncatewords:10 }}</div>
                                {% endif %}
                            </div>
                        </div>
                    </td>
                    <td class="px-6 py-4 whitespace-nowrap">
                        <span class="px-2 py-1 text-xs font-medium rounded-full bg-theme-100 text-theme-800">
                            {{ config.get_provider_display }}
                        </span>
                    </td>
                    <td class="px-6 py-4 whitespace-nowrap">
                        <div class="text-sm text-gray-900">{{ config.model_name }}</div>
                        {% if config.api_base %}
                        <div class="text-xs text-gray-500">自定义端点</div>
                        {% endif %}
                    </td>
                    <td class="px-6 py-4 whitespace-nowrap">
                        <div class="text-xs text-gray-600">
                            <div>Temperature: {{ config.temperature }}</div>
                            <div>Max Tokens: {{ config.max_tokens }}</div>
                            <div>Timeout: {{ config.timeout }}s</div>
                        </div>
                    </td>
                    <td class="px-6 py-4 whitespace-nowrap">
                        <div class="text-xs text-gray-600">
                            <div>请求: {{ config.total_requests }}</div>
                            <div>Token: {{ config.total_tokens|default:0 }}</div>
                            {% if config.last_used_at %}
                            <div class="text-gray-400">{{ config.last_used_at|date:"Y-m-d H:i" }}</div>
                            {% else %}
                            <div class="text-gray-400">未使用</div>
                            {% endif %}
                        </div>
                    </td>
                    <td class="px-6 py-4 whitespace-nowrap">
                        {% if config.is_active %}
                        <span class="px-2 py-1 text-xs font-medium rounded-full bg-green-100 text-green-800">
                            <svg x-data="heroicon('check-circle')" class="mr-1" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" x-cloak><span x-html="svg"></span></svg>启用
                        </span>
                        {% else %}
                        <span class="px-2 py-1 text-xs font-medium rounded-full bg-gray-100 text-gray-800">
                            <svg x-data="heroicon('x-circle')" class="mr-1" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" x-cloak><span x-html="svg"></span></svg>禁用
                        </span>
                        {% endif %}
                    </td>
                    <td class="px-6 py-4 whitespace-nowrap text-sm font-medium">
                        <div class="flex items-center space-x-2">
                            <a href="{% url 'ai_assistant:model_config_test' config.pk %}"
                               class="text-theme-600 hover:text-theme-900"
                               title="测试连接">
                                <i class="fas fa-plug"></i>
                            </a>
                            <a href="{% url 'ai_assistant:model_config_edit' config.pk %}"
                               class="text-indigo-600 hover:text-indigo-900"
                               title="编辑">
                                <svg x-data="heroicon('pencil')" class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" x-cloak><span x-html="svg"></span></svg>
                            </a>
                            {% if not config.is_default %}
                            <form method="post" action="{% url 'ai_assistant:model_config_set_default' config.pk %}"
                                  class="inline"
                                  onsubmit="return confirm('确定要将此配置设为默认吗？');">
                                {% csrf_token %}
                                <button type="submit"
                                        class="text-yellow-600 hover:text-yellow-900"
                                        title="设为默认">
                                    <svg x-data="heroicon('star')" class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" x-cloak><span x-html="svg"></span></svg>
                                </button>
                            </form>
                            <form method="post" action="{% url 'ai_assistant:model_config_delete' config.pk %}"
                                  class="inline"
                                  onsubmit="return confirm('确定要删除此配置吗？');">
                                {% csrf_token %}
                                <button type="submit"
                                        class="text-red-600 hover:text-red-900"
                                        title="删除">
                                    <svg x-data="heroicon('trash')" class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" x-cloak><span x-html="svg"></span></svg>
                                </button>
                            </form>
                            {% endif %}
                        </div>
                    </td>
                </tr>
                {% empty %}
                <tr>
                    <td colspan="7" class="px-6 py-12 text-center text-gray-500">
                        <svg x-data="heroicon('inbox')" class="text-4xl mb-3 text-gray-300" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" x-cloak><span x-html="svg"></span></svg>
                        <p>暂无AI模型配置</p>
                        <a href="{% url 'ai_assistant:model_config_create' %}"
                           class="inline-block mt-3 text-theme-600 hover:text-theme-700">
                            <svg x-data="heroicon('plus')" class="mr-1" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" x-cloak><span x-html="svg"></span></svg>立即创建
                        </a>
                    </td>
                </tr>
                {% endfor %}
            </tbody>
        </table>
    </div> <!-- Pagination -->
    {% if page_obj.has_other_pages %}
    <div class="bg-white rounded-lg shadow px-4 py-3 flex items-center justify-between sm:px-6">
        <div class="flex-1 flex justify-between sm:hidden">
            {% if page_obj.has_previous %}
            <a href="?page={{ page_obj.previous_page_number }}&search={{ search }}&provider={{ current_provider }}&is_active={{ current_is_active }}"
               class="relative inline-flex items-center px-4 py-2 border border-gray-300 text-sm font-medium rounded-md text-gray-700 bg-white hover:bg-gray-50">
                上一页
            </a>
            {% endif %}
            {% if page_obj.has_next %}
            <a href="?page={{ page_obj.next_page_number }}&search={{ search }}&provider={{ current_provider }}&is_active={{ current_is_active }}"
               class="ml-3 relative inline-flex items-center px-4 py-2 border border-gray-300 text-sm font-medium rounded-md text-gray-700 bg-white hover:bg-gray-50">
                下一页
            </a>
            {% endif %}
        </div>
        <div class="hidden sm:flex-1 sm:flex sm:items-center sm:justify-between">
            <div>
                <p class="text-sm text-gray-700">
                    显示第 <span class="font-medium">{{ page_obj.start_index }}</span> 到
                    <span class="font-medium">{{ page_obj.end_index }}</span> 条，
                    共 <span class="font-medium">{{ page_obj.paginator.count }}</span> 条记录
                </p>
            </div>
            <div>
                <nav class="relative z-0 inline-flex rounded-md shadow-sm -space-x-px">
                    {% if page_obj.has_previous %}
                    <a href="?page={{ page_obj.previous_page_number }}&search={{ search }}&provider={{ current_provider }}&is_active={{ current_is_active }}"
                       class="relative inline-flex items-center px-2 py-2 rounded-l-md border border-gray-300 bg-white text-sm font-medium text-gray-500 hover:bg-gray-50">
                        <svg x-data="heroicon('chevron-left')" class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" x-cloak><span x-html="svg"></span></svg>
                    </a>
                    {% endif %} {% for num in page_obj.paginator.page_range %}
                        {% if page_obj.number == num %}
                        <span class="relative inline-flex items-center px-4 py-2 border border-theme-600 bg-theme-50 text-sm font-medium text-theme-600">
                            {{ num }}
                        </span>
                        {% elif num > page_obj.number|add:'-3' and num < page_obj.number|add:'3' %}
                        <a href="?page={{ num }}&search={{ search }}&provider={{ current_provider }}&is_active={{ current_is_active }}"
                           class="relative inline-flex items-center px-4 py-2 border border-gray-300 bg-white text-sm font-medium text-gray-700 hover:bg-gray-50">
                            {{ num }}
                        </a>
                        {% endif %}
                    {% endfor %} {% if page_obj.has_next %}
                    <a href="?page={{ page_obj.next_page_number }}&search={{ search }}&provider={{ current_provider }}&is_active={{ current_is_active }}"
                       class="relative inline-flex items-center px-2 py-2 rounded-r-md border border-gray-300 bg-white text-sm font-medium text-gray-500 hover:bg-gray-50">
                        <svg x-data="heroicon('chevron-right')" class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" x-cloak><span x-html="svg"></span></svg>
                    </a>
                    {% endif %}
                </nav>
            </div>
        </div>
    </div>
    {% endif %}
</div>
{% endblock %}
