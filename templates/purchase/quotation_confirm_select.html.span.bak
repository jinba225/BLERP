{% extends 'base.html' %}
{% block title %}选定报价 - {{ quotation.quotation_number }} - BetterLaser ERP{% endblock %}

{% block breadcrumb %}
<span class="mx-2 text-gray-400">/</span>
<a href="{% url 'purchase:quotation_list' %}" class="text-gray-500 hover:text-gray-700">供应商报价</a>
<span class="mx-2 text-gray-400">/</span>
<a href="{% url 'purchase:quotation_detail' quotation.pk %}" class="text-gray-500 hover:text-gray-700">{{ quotation.quotation_number }}</a>
<span class="mx-2 text-gray-400">/</span>
<span class="text-gray-900 font-semibold">选定报价</span>
{% endblock %}

{% block content %}
<div class="max-w-4xl mx-auto space-y-6">
    <div class="bg-white rounded-lg shadow p-6">
        <div class="flex items-center mb-6">
            <div class="flex-shrink-0 bg-green-100 rounded-full p-3">
                <svg x-data="heroicon('check-circle')" class="text-green-600 text-2xl" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" x-cloak><span x-html="svg"></span></svg>
            </div>
            <div class="ml-4">
                <h3 class="text-lg font-semibold text-gray-900">确认选定此报价</h3>
                <p class="text-sm text-gray-600 mt-1">选定后，其他供应商的报价将被自动拒绝</p>
            </div>
        </div> <!-- Quotation Info -->
        <div class="border-t border-gray-200 pt-6 space-y-4">
            <div class="grid grid-cols-2 gap-4">
                <div>
                    <span class="text-sm text-gray-600">报价单号</span>
                    <p class="text-sm font-medium text-gray-900 mt-1">{{ quotation.quotation_number }}</p>
                </div>
                <div>
                    <span class="text-sm text-gray-600">供应商</span>
                    <p class="text-sm font-medium text-gray-900 mt-1">{{ quotation.supplier.name }}</p>
                </div>
                <div>
                    <span class="text-sm text-gray-600">报价日期</span>
                    <p class="text-sm font-medium text-gray-900 mt-1">{{ quotation.quotation_date }}</p>
                </div>
                <div>
                    <span class="text-sm text-gray-600">有效期至</span>
                    <p class="text-sm font-medium text-gray-900 mt-1">{{ quotation.valid_until }}</p>
                </div>
                <div>
                    <span class="text-sm text-gray-600">交货期</span>
                    <p class="text-sm font-medium text-gray-900 mt-1">
                        {% if quotation.delivery_period %}{{ quotation.delivery_period }} 天{% else %}<span class="text-gray-400">-</span>{% endif %}
                    </p>
                </div>
                <div>
                    <span class="text-sm text-gray-600">报价总额</span>
                    <p class="text-lg font-bold text-green-600 mt-1">¥{{ quotation.total_amount|floatformat:2 }}</p>
                </div>
            </div>
        </div> <!-- Items Summary -->
        <div class="border-t border-gray-200 pt-6 mt-6">
            <h4 class="text-md font-semibold text-gray-900 mb-4">报价明细</h4>
            <div class="overflow-x-auto">
                <table class="min-w-full divide-y divide-gray-200 border border-gray-200 rounded-lg">
                    <thead class="bg-gray-50">
                        <tr>
                            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">产品名称</th>
                            <th class="px-6 py-3 text-right text-xs font-medium text-gray-500 uppercase tracking-wider">数量</th>
                            <th class="px-6 py-3 text-right text-xs font-medium text-gray-500 uppercase tracking-wider">单价</th>
                            <th class="px-6 py-3 text-right text-xs font-medium text-gray-500 uppercase tracking-wider">小计</th>
                        </tr>
                    </thead>
                    <tbody class="bg-white divide-y divide-gray-200">
                        {% for item in items %}
                        <tr>
                            <td class="px-6 py-4 text-sm text-gray-900">
                                <div class="font-medium">{{ item.product.name }}</div>
                                <div class="text-xs text-gray-500">{{ item.product.code }}</div>
                            </td>
                            <td class="px-6 py-4 text-sm text-right text-gray-900">
                                {{ item.quantity }} {{ item.product.unit }}
                            </td>
                            <td class="px-6 py-4 text-sm text-right text-gray-900">
                                ¥{{ item.unit_price|floatformat:2 }}
                            </td>
                            <td class="px-6 py-4 text-sm text-right font-medium text-gray-900">
                                ¥{{ item.line_total|floatformat:2 }}
                            </td>
                        </tr>
                        {% endfor %}
                    </tbody>
                </table>
            </div>
        </div> <form method="post" class="border-t border-gray-200 pt-6 mt-6 space-y-4">
            {% csrf_token %}
            <div>
                <label class="block text-sm font-medium text-gray-700 mb-2">综合评分 (0-10)</label>
                <input type="number" name="evaluation_score" min="0" max="10" step="0.1" class="w-full px-3 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-theme-500" placeholder="请输入综合评分">
                <p class="text-xs text-gray-500 mt-1">根据价格、质量、交货期等因素给出综合评分</p>
            </div>
            <div>
                <label class="block text-sm font-medium text-gray-700 mb-2">评估备注</label>
                <textarea name="evaluation_notes" rows="3" class="w-full px-3 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-theme-500" placeholder="记录选定此报价的理由..."></textarea>
            </div> <div class="bg-yellow-50 border-l-4 border-yellow-400 p-4">
                <div class="flex">
                    <div class="flex-shrink-0">
                        <svg x-data="heroicon('exclamation-triangle')" class="text-yellow-400" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" x-cloak><span x-html="svg"></span></svg>
                    </div>
                    <div class="ml-3">
                        <p class="text-sm text-yellow-700">
                            选定此报价后，同一询价单的其他报价将被自动拒绝，此操作不可撤销
                        </p>
                    </div>
                </div>
            </div> <div class="flex justify-end space-x-3">
                <a href="{% url 'purchase:quotation_detail' quotation.pk %}" class="px-4 py-2 border border-gray-300 text-gray-700 rounded-lg hover:bg-gray-50 transition-colors">
                    <svg x-data="heroicon('x-mark')" class="mr-2" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" x-cloak><span x-html="svg"></span></svg>取消
                </a>
                <button class="btn btn-primary" type="submit">
                    <svg x-data="heroicon('check-circle')" class="mr-2" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" x-cloak><span x-html="svg"></span></svg>确认选定
                </button>
            </div>
        </form>
    </div>
</div>
{% endblock %}
