{% extends 'base.html' %}
{% block title %}删除询价单 - {{ inquiry.inquiry_number }} - BetterLaser ERP{% endblock %}

{% block breadcrumb %}
<span class="mx-2 text-gray-400">/</span>
<a href="{% url 'purchase:inquiry_list' %}" class="text-gray-500 hover:text-gray-700">询价管理</a>
<span class="mx-2 text-gray-400">/</span>
<a href="{% url 'purchase:inquiry_detail' inquiry.pk %}" class="text-gray-500 hover:text-gray-700">{{ inquiry.inquiry_number }}</a>
<span class="mx-2 text-gray-400">/</span>
<span class="text-gray-900 font-semibold">删除确认</span>
{% endblock %}

{% block content %}
<div class="max-w-2xl mx-auto space-y-6">
    <div class="bg-white rounded-lg shadow p-6">
        <div class="flex items-center mb-6">
            <div class="flex-shrink-0 bg-red-100 rounded-full p-3">
                <svg x-data="heroicon('exclamation-triangle')" class="text-red-600 text-2xl" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" x-cloak><span x-html="svg"></span></svg>
            </div>
            <div class="ml-4">
                <h3 class="text-lg font-semibold text-gray-900">确认删除询价单</h3>
                <p class="text-sm text-gray-600 mt-1">此操作将删除询价单及所有相关数据</p>
            </div>
        </div> <div class="border-t border-gray-200 pt-6 space-y-4">
            <div class="grid grid-cols-2 gap-4">
                <div>
                    <span class="text-sm text-gray-600">询价单号</span>
                    <p class="text-sm font-medium text-gray-900 mt-1">{{ inquiry.inquiry_number }}</p>
                </div>
                <div>
                    <span class="text-sm text-gray-600">标题</span>
                    <p class="text-sm font-medium text-gray-900 mt-1">{{ inquiry.title }}</p>
                </div>
                <div>
                    <span class="text-sm text-gray-600">询价日期</span>
                    <p class="text-sm font-medium text-gray-900 mt-1">{{ inquiry.inquiry_date }}</p>
                </div>
                <div>
                    <span class="text-sm text-gray-600">状态</span>
                    <p class="text-sm font-medium text-gray-900 mt-1">{{ inquiry.get_status_display }}</p>
                </div>
            </div> <div class="bg-yellow-50 border-l-4 border-yellow-400 p-4">
                <div class="flex">
                    <div class="flex-shrink-0">
                        <svg x-data="heroicon('exclamation-circle')" class="text-yellow-400" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" x-cloak><span x-html="svg"></span></svg>
                    </div>
                    <div class="ml-3">
                        <p class="text-sm text-yellow-700">
                            删除后将无法恢复询价单数据及所有明细记录
                        </p>
                    </div>
                </div>
            </div> <div class="mt-6 flex justify-end space-x-3">
                <a href="{% url 'purchase:inquiry_detail' inquiry.pk %}" class="px-4 py-2 border border-gray-300 text-gray-700 rounded-lg hover:bg-gray-50 transition-colors">
                    <svg x-data="heroicon('x-mark')" class="mr-2" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" x-cloak><span x-html="svg"></span></svg>取消
                </a>
                <form method="post" class="inline">
                    {% csrf_token %}
                    <button class="btn btn-primary" type="submit">
                        <svg x-data="heroicon('trash')" class="mr-2" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" x-cloak><span x-html="svg"></span></svg>确认删除
                    </button>
                </form>
            </div>
        </div>
    </div>
</div>
{% endblock %}
